{
  "name": "iframe-overlay-sdk",
  "version": "1.0.0",
  "description": "SDK for tracking elements in iframe and rendering overlays in host page",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  }
}
